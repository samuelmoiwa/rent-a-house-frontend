import React from 'react';
import LogoutButton from './auth/Logout';
import MainPage from './MainPage';

const Home = () => (
  <div>
    <LogoutButton />
    <MainPage />
    
  </div>
);

export default Home;
