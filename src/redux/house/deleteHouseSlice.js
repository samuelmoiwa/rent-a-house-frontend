import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    deleteHouse: [],
    isLoading: false,
    error: null,
};

const deleteHouseSlice = createSlice({
    name: 'deleteHouse',
    initialState,
    reducers: {
        
    }
})
